SELECT new_person_order.order_date,
    person.name || ' (age:' || person.age || ')' AS person_information
FROM (SELECT order_date, person_id AS id FROM person_order) AS new_person_order
NATURAL JOIN person
ORDER BY order_date, person_information;
